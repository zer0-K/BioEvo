#pragma once

#include <vector>
#include <string>
#include <cstring>
#include "../evox_universe.hpp"
#include "level_config.hpp"

namespace back::level
{

    LevelConfig* get_level_config(std::string experiment_name)
    {

    }

} // namespace back::level
